<UserControl x:Class="TaskOrganizer.View.TodoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskOrganizer.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Column="2" Background="BlanchedAlmond">
            <Label Content="ToDo List" HorizontalAlignment="Center" FontFamily="Fira Code" FontSize="30" />
            <Label Content="Enter a new task:" HorizontalAlignment="Left" Margin="20,0,0,0"/>
            <TextBox Width="150" Height="80" Text="{Binding TodoViewModel.NewTask, Mode=TwoWay}">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <Grid Name="gridButton" Width="120" Height="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Add" Command="{Binding TodoViewModel.AddNewTaskCommand}"/>
                <Button Grid.Column="1" Content="Delete"/>
            </Grid>

            <Grid Height="500">
                <StackPanel Margin="0,10">
                    <TextBlock Text="Your tasks:" Margin="10" HorizontalAlignment="Left"/>
                    <ScrollViewer Height="330" Margin="0,0,0,0">
                        <ItemsControl ItemsSource="{Binding TodoViewModel.TodoList}" Background="White">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Grid>


        </StackPanel>

    </Grid>
</UserControl>
